sequenceDiagram
    participant browser
    participant server

    Note right of browser: User clicks Save. JavaScript intercepts the form submit event using e.preventDefault().
    Note right of browser: JS creates a new note object locally, adds it to the list, and re-renders the UI immediately.
    
    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note left of server: Server parses the JSON payload and saves the new note to the database.
    server-->>browser: HTTP 201 Created (with the created note as JSON)
    deactivate server
    
    Note right of browser: No redirect occurs. The browser stays on the same page.
